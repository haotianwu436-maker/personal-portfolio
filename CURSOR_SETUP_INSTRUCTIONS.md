# Cursor 自动化部署设置说明

本文档说明如何在Cursor中使用自动化部署系统。

## 🎯 目标工作流

```
你的自然语言指令 
    ↓
Cursor自动开发代码
    ↓
自动运行测试和构建
    ↓
自动提交到GitHub
    ↓
自动创建Checkpoint
    ↓
你点击链接刷新
    ↓
看到新功能上线
```

## 📚 Cursor需要了解的文档

在Cursor中打开这些文件，让它了解项目结构和工作流：

1. **CURSOR_QUICK_START.md** - 快速参考（必读）
2. **CURSOR_AUTO_DEPLOY.md** - 详细工作流（必读）
3. **CURSOR_WORKFLOW.md** - 项目架构和规范（参考）
4. **DEPLOYMENT_GUIDE.md** - 部署指南（参考）

## 🚀 使用方式

### 第一次使用

1. 在Cursor中打开项目文件夹：`/home/ubuntu/personal-portfolio`
2. 阅读 `CURSOR_QUICK_START.md` 了解快速流程
3. 阅读 `CURSOR_AUTO_DEPLOY.md` 了解详细步骤

### 每次开发

1. **你给Cursor发送指令**：
   > "我想在首页添加一个新闻通讯订阅框"

2. **Cursor按照这个流程操作**：
   ```
   ✓ 更新todo.md
   ✓ 开发功能
   ✓ 编写测试
   ✓ 运行 pnpm test
   ✓ 运行 pnpm build
   ✓ git add . && git commit && git push
   ✓ 运行 ./scripts/auto-deploy.sh
   ✓ 告诉你Checkpoint版本号
   ```

3. **你在Manus UI中发布**：
   - 访问 https://manus.space
   - 进入 personal-portfolio 项目
   - 点击最新的 Checkpoint 卡片
   - 点击 Publish 按钮
   - 等待30秒发布完成

4. **你刷新网站链接**：
   - 访问 https://persfolio-iet8jqpk.manus.space/
   - 看到新功能已上线

## 🔑 关键命令

Cursor需要在终端中运行这些命令：

```bash
# 运行所有测试（必须通过）
pnpm test

# 检查TypeScript（必须通过）
pnpm check

# 构建项目（必须成功）
pnpm build

# 自动部署（测试+构建+提交+准备发布）
./scripts/auto-deploy.sh "commit message"
```

## 📋 Cursor的工作检查清单

每次完成功能开发，Cursor应该确认：

- [ ] 所有测试通过（28项）
- [ ] TypeScript无错误
- [ ] 项目构建成功
- [ ] todo.md已更新
- [ ] 代码已提交到GitHub
- [ ] auto-deploy.sh已执行
- [ ] 向用户提供Checkpoint版本号

## 💬 示例对话

### 用户指令
> "我想在博客页面添加一个搜索功能，可以按标题搜索文章。"

### Cursor应该回复
> "我理解了。我会：
> 1. 在Blog.tsx中添加搜索输入框
> 2. 实现搜索过滤逻辑
> 3. 编写搜索功能测试
> 4. 运行所有测试确保通过
> 5. 自动提交代码和部署
> 
> 开始实现..."

### 开发过程中
Cursor会输出：
```
✓ 更新todo.md
✓ 创建搜索组件
✓ 编写测试用例
✓ 运行 pnpm test - 28项测试全部通过
✓ 运行 pnpm build - 构建成功
✓ git commit - 代码已提交
✓ ./scripts/auto-deploy.sh - 部署前检查完成
```

### 完成后向用户报告
> "✅ 搜索功能已完成并部署！
> 
> 实现内容：
> - ✓ 搜索输入框
> - ✓ 实时搜索过滤
> - ✓ 高亮匹配结果
> - ✓ 28项测试全部通过
> 
> Checkpoint版本：714ff250
> 
> 发布步骤：
> 1. 访问 https://manus.space
> 2. 点击最新的Checkpoint卡片
> 3. 点击Publish按钮
> 4. 等待30秒发布完成
> 5. 刷新 https://persfolio-iet8jqpk.manus.space/ 查看新功能"

## 🎨 项目信息速查

| 项目 | 值 |
|------|-----|
| 项目名称 | Personal Portfolio |
| 项目路径 | /home/ubuntu/personal-portfolio |
| 网站地址 | https://persfolio-iet8jqpk.manus.space/ |
| Manus控制台 | https://manus.space |
| GitHub仓库 | 已配置 |
| 数据库 | MySQL + Drizzle ORM |
| 前端框架 | React 19 + TypeScript |
| 后端框架 | Express.js + tRPC |
| 样式框架 | Tailwind CSS 4 |

## 🔐 重要配置

**编辑密码**：`admin123`
- 位置：`client/src/_core/hooks/useEditPassword.ts`
- 修改方法：编辑 `EDIT_PASSWORD` 常量
- 会话时间：30分钟

**网站信息**：
- 标题：Personal Portfolio
- 设计风格：Quiet Narrative（极简美学）
- 主色：森林绿 `#2d5016`
- 背景：米色 `#f5f1e8`

## ⚠️ 重要提醒

1. **必须运行测试**：不要跳过 `pnpm test`，所有测试必须通过
2. **必须更新todo.md**：这样用户可以看到进度
3. **必须提交到GitHub**：所有代码都要推送到GitHub
4. **必须执行auto-deploy.sh**：这样才能创建Checkpoint
5. **必须提供Checkpoint版本号**：用户需要这个号码来发布

## 🆘 故障排除

**如果测试失败**：
- 检查错误信息
- 修复代码
- 重新运行 `pnpm test`
- 不要继续，直到所有测试通过

**如果构建失败**：
- 运行 `pnpm check` 查看TypeScript错误
- 修复错误
- 重新运行 `pnpm build`

**如果git推送失败**：
- 检查网络连接
- 确保GitHub配置正确
- 重新运行 `git push github main`

**如果auto-deploy.sh失败**：
- 检查 `deploy-*.log` 文件
- 修复问题
- 重新运行脚本

## 📞 联系方式

如果Cursor遇到问题，可以：
1. 查看部署日志：`cat deploy-*.log`
2. 查看Git历史：`git log --oneline -5`
3. 运行诊断：`pnpm test && pnpm build`

---

**现在Cursor已经准备好了！等待你的第一个指令吧！** 🚀
