# Cursor 快速参考指南

这是一份简化版的Cursor工作指南，适合快速查阅。详细内容请参考 `CURSOR_AUTO_DEPLOY.md`。

## 🚀 完整工作流（5分钟版本）

### 1️⃣ 接收用户指令
用户告诉你需要什么功能。例如：
> "我想在首页添加一个订阅邮箱的表单"

### 2️⃣ 开发功能
按照这个顺序：
1. 更新 `todo.md`，添加任务
2. 编写代码（遵循项目规范）
3. 编写测试（如果是API功能）
4. 运行 `pnpm test` 确保通过
5. 运行 `pnpm build` 确保成功

### 3️⃣ 提交代码
```bash
git add .
git commit -m "feat: add newsletter subscription"
git push github main
```

### 4️⃣ 部署
```bash
./scripts/auto-deploy.sh "feat: add newsletter subscription"
```

### 5️⃣ 发布
访问 https://manus.space → 点击 Publish → 完成！

---

## 📋 项目结构速查

| 位置 | 用途 |
|------|------|
| `client/src/pages/` | 页面组件 |
| `client/src/components/` | UI组件 |
| `client/src/_core/hooks/` | 自定义Hooks |
| `server/routers.ts` | API路由 |
| `server/db.ts` | 数据库查询 |
| `drizzle/schema.ts` | 数据库表定义 |
| `server/*.test.ts` | 单元测试 |
| `todo.md` | 任务清单 |

---

## 🎨 设计规范速查

| 项目 | 值 |
|------|-----|
| 主色（强调色） | 森林绿 `#2d5016` |
| 背景色 | 米色 `#f5f1e8` |
| 文字色 | 深灰色 `#2c2c2c` |
| 组件库 | shadcn/ui |
| 样式框架 | Tailwind CSS 4 |
| 图标库 | lucide-react |

---

## 🔐 密码和配置

| 项目 | 位置 | 默认值 |
|------|------|--------|
| 编辑密码 | `client/src/_core/hooks/useEditPassword.ts` | `admin123` |
| 密码过期时间 | 同上 | 30分钟 |
| 网站标题 | Manus UI Settings | Personal Portfolio |
| API基础URL | 环境变量 | `VITE_FRONTEND_FORGE_API_URL` |

---

## ✅ 必做检查清单

每次开发完成前，必须检查：

- [ ] 所有测试通过 (`pnpm test`)
- [ ] TypeScript无错误 (`pnpm check`)
- [ ] 项目能构建 (`pnpm build`)
- [ ] todo.md已更新
- [ ] 代码已提交到GitHub
- [ ] 脚本已执行 (`./scripts/auto-deploy.sh`)
- [ ] 网站已发布到Manus

---

## 🔧 常用命令

```bash
# 运行测试
pnpm test

# 检查TypeScript
pnpm check

# 构建项目
pnpm build

# 启动开发服务器
pnpm dev

# 自动部署（测试+构建+提交+准备发布）
./scripts/auto-deploy.sh "commit message"

# 查看部署日志
cat deploy-*.log

# 查看最近的commit
git log --oneline -5
```

---

## 📝 Commit Message规范

格式：`type: description`

类型：
- `feat` - 新功能
- `fix` - 修复bug
- `docs` - 文档
- `refactor` - 重构
- `test` - 测试
- `style` - 样式

示例：
```
feat: add newsletter subscription feature

- Create newsletter database table
- Implement subscription API endpoint
- Build Newsletter React component
- Add subscription tests
```

---

## 🚨 常见问题速解

**Q: 测试失败了怎么办？**
A: 修复代码，重新运行 `pnpm test`

**Q: 构建失败了怎么办？**
A: 检查TypeScript错误，运行 `pnpm check` 查看详情

**Q: 怎么修改编辑密码？**
A: 编辑 `client/src/_core/hooks/useEditPassword.ts`，修改 `EDIT_PASSWORD` 常量

**Q: 怎么添加新的数据库表？**
A: 在 `drizzle/schema.ts` 中定义，然后运行 `pnpm db:push`

**Q: 怎么添加新的API端点？**
A: 在 `server/routers.ts` 中添加，在 `server/db.ts` 中实现数据库操作

**Q: 部署失败了怎么办？**
A: 脚本会自动回滚。检查 `deploy-*.log` 文件查看错误原因

---

## 🎯 成功标志

完成一个功能后，你应该看到：

✅ 所有28项测试通过
✅ TypeScript编译无错误
✅ 项目构建成功
✅ 代码推送到GitHub
✅ 网站已发布到Manus
✅ 新功能在网站上可见

---

## 📚 详细文档

- **完整工作流**：`CURSOR_AUTO_DEPLOY.md`
- **项目架构**：`CURSOR_WORKFLOW.md`
- **部署指南**：`DEPLOYMENT_GUIDE.md`
- **自动化设置**：`AUTOMATION_SETUP.md`

---

## 💡 提示

1. **始终更新todo.md**：这样用户可以看到进度
2. **写好commit message**：清晰的提交信息便于追踪
3. **运行所有测试**：不要跳过这一步
4. **检查代码风格**：保持代码一致性
5. **及时反馈**：告诉用户完成情况

---

**准备好了吗？开始接收用户指令吧！** 🚀
